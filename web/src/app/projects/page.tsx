import { PagePlaceholder } from "@/components/page-placeholder";

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      phase="Phase 3 (case-study content model + role filtering)"
    />
  );
}
